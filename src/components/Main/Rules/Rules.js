import React from "react";

import "./Rules.css";

const Rules = () => {
  return <div className="rules">this is rules
    rules rules rulesrules rules rulesrules rules rules rules rules rulesrules rules rules <br/>
    rules rules rulesrules rules rulesrules rules rules rules rules rulesrules rules rules <br/>
    rules rules rulesrules rules rulesrules rules rules rules rules rulesrules rules rules <br/>
    rules rules rulesrules rules rulesrules rules rules rules rules rulesrules rules rules <br/>
    rules rules rulesrules rules rulesrules rules rules rules rules rulesrules rules rules <br/></div>;
};

export default Rules;
